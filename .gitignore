# kbuild artifacts
*.ko
*.mod
*.mod.c
*.o
.*.cmd
*.symvers
*.order
.tmp_versions/
# IDE / tooling
.idea/
.claude/
# RE source material (large vendor binaries) - kept local, not committed
windowsDriver/
# Third-party GPL drivers consulted during RE. Never built, never distributed,
# and nothing in src/ derives from them - see README "Provenance". Safe to
# delete; kept only so the RE citations in RE_FINDINGS.md can be checked.
reference/
# RE-derived vendor disasm/dumps - kept local
re-dump/
ep-disasm.txt
__pycache__/
